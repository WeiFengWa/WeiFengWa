import { Plugin } from 'vue'
import { WfIcon } from './icon'
import { WfButton, WfButtonGroup } from './button'

export default [WfIcon, WfButton, WfButtonGroup] as Plugin[]

export { WfIcon, WfButton, WfButtonGroup }
